name: Deploy to server 

on: 
  push: 
    branches: ["M2C1-YAMLyWorkflows"]

jobs: 
  #First job
  first_job:
    runs-on: ubuntu-latest
    env:
      NAME: CodigoFacilito
    steps:
      #List of steaps
      - name: Hola mundo
        run: echo "Hola mundo $NAME"

  second_job:
    if: ${{ github.ref == 'refs/heads/M2C1-YAMLyWorkflows'}}
    needs:
      - first_job #Depens on first job, wait to first jobs ends success

    runs-on: ubuntu-latest

    steps:
      - name: Hola mundo 2
        run: echo "Hola mundo 2"